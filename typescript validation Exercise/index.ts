export interface userNameProcess{
    usernameFunction(username:string):boolean
}
export interface emailprocess{
     emailFunction(email:string):boolean
}
export interface passwordProcess{
    passwordFunction(password:string):boolean
}
