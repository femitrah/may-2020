const student = {
        name: 'raja',
        age: 12,
        standard: 7,
        sports: 'football',
        rank: 8
};
const keys = ['name','age','standard', 'rank'];
const keys1=[student['name'],student['age'],student['standard'],student['rank']]
for (let i=0;i<keys.length;i++){
console.log(`${keys[i]}:${keys1[i]}`)
}








